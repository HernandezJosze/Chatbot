# Example of a .env file, the next values next to be set before you run the chatbot
# Both env files must match the the first 4 variables
ME_CONFIG_MARIADB_ADMINUSERNAME=SustituyePorUnUsuario
ME_CONFIG_MARIADB_ADMINPASSWORD=SustituyePorContraseña
ME_CONFIG_MARIADB_HOST=database #This variable should match the name of the service of MariaDB as it appears in the docker-compose file
ME_CONFIG_MARIADB_DATABASE=SustituteNombreBaseDeDatos # could be something like "db_data"